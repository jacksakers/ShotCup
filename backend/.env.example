# Passwords and secrets — never commit this file
SITE_PASSWORD=familypassword
ADMIN_PASSWORD=adminpassword
SECRET_KEY=replace-with-a-long-random-string

# Optional: override database file location (default: shotcup.db in backend/)
# DB_PATH=shotcup.db
